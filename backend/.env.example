# -----------------------------
# QuizGen Google Auth (OAuth)
# -----------------------------
# Create OAuth credentials in Google Cloud Console:
# APIs & Services -> Credentials -> OAuth client ID (Web application)
GOOGLE_OAUTH_CLIENT_ID=123456789012-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-your-client-secret

# Generate once using OAuth consent + offline access. This should be a long-lived token.
GOOGLE_OAUTH_REFRESH_TOKEN=1//0gExampleRefreshTokenValue

# Optional: leave empty; backend refreshes from refresh token automatically.
GOOGLE_OAUTH_ACCESS_TOKEN=

# -----------------------------
# Optional publish behavior
# -----------------------------
# If set, created forms are shared with this Google account as writer.
QUIZGEN_USER_EMAIL=teacher@example.com

# If set, created forms are moved into this Drive folder.
QUIZGEN_FOLDER_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz123456

# -----------------------------
# Service account fallback (legacy)
# -----------------------------
# Used only when OAuth vars above are missing.
GOOGLE_SERVICE_ACCOUNT_FILE=service_account.json
